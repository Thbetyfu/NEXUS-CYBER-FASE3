# Handoff Red → Blue — sesi 16 Agu 2026

Laptop Red: Wi-Fi `NEXUS-BLUE-LAB`, IP `192.168.137.66`, WAF `http://192.168.137.1`.

## Yang sudah lulus (wajib checklist)

| Item | Bukti |
| --- | --- |
| Pintu WAF | Portofolio 200 setelah PoW (bukan Vercel) |
| SOC `:3001` / `:8081` | Tertutup dari Red |
| Postgres `:5432` / Redis `:6379` | Tertutup |
| Unggah PNG lab (~1,2 MB) | `POST /api/upload` → 200, `status:success`, `/api/guest-photos/c57eb232-4d34-49c0-bda2-73e61e350491` (GET 200) |
| Vault 5× salah | Autoban; UI “IP permanently blacklisted”; lalu unban Blue |
| NEX-RED blackbox | Scan `NEXRED-77264007`, 11 probe, 7 blok defensif, `--no-llm`. Bukan pentest Shannon |

## Yang Blue perlu lakukan di laptop THORIQ

1. `git pull origin main --recurse-submodules` (header WAF + Caddy lab).
2. `STOP.bat` lalu `START-OFFLINE.bat` (rebuild gateway/Caddy) agar header `nosniff` / `X-Frame-Options` tampil di tantangan 403.
3. **Jangan** pasang HSTS di HTTP hotspot.
4. Opsional dataset NEX-AI (label dari log WAF, bukan LLM):

```bat
python NEX-AI\scripts\collect_lab_dataset.py
```

5. Kalau list Gallery origin masih 0: cek `GET /api/photos` lewat WAF (store gateway). UI origin perlu fetch itu setelah rebuild image `portfolio`.

## Jangan diklaim

eBPF XDP, JWT enterprise, Stripe, NEX-RED = Shannon/Strix, atau “AI penuh” pada setiap request.
